# Usage

```bash
dotnet run --project MarketData -- -h
dotnet run --project MarketData -- list
dotnet run --project MarketData -- data -s BTCUSDT -b 2024-01-01
```

TODO:
参考Lean的ToolBox，按照它那边的标准重构
需要研究一下Lean的数据结构：https://github.com/QuantConnect/Lean.DataSource.SDK
